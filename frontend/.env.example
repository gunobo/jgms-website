# NOTE: docker-compose.yml overrides VITE_API_URL with the value from the root
# .env (defaults to localhost:8001 for dev). No need to edit it here.
VITE_API_URL="http://localhost:8001"
VITE_GOOGLE_CLIENT_ID=""
